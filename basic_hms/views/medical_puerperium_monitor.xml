<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="medical_puerperium_monitor_tree_view" model="ir.ui.view">
        <field name="name">medical.puerperium.monitor.tree.view</field>
        <field name="model">medical.puerperium.monitor</field>
        <field name="arch" type="xml">
            <list string="Monitoreo del Puerperio">
                <field name="date" string="Fecha" />
                <field name="systolic_pressure" string="Presión Sistólica" />
                <field name="diastolic_pressure" string="Presión Diastólica" />
                <field name="heart_freq" string="Frecuencia Cardíaca" />
                <field name="temprature" string="Temperatura" />
                <field name="fundal_height" string="Altura del Fondo Uterino" />
                <field name="lochia_amount" string="Cantidad de Lochia" />
                <field name="lochia_color" string="Color de Lochia" />
                <field name="loicha_order" string="Orden de Lochia" />
            </list>
        </field>
    </record>

</odoo>
